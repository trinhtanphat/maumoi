# Deployment

Production target: Cloudflare account `46646bc5777a895df590614528828dd2`, Worker `maumoi`, D1 `maumoi-db`, R2 `maumoi-evidence`. Deploy exact merged `main` source only after tests/checks pass. Zalo production OTP requires approved credentials/template and is not faked when unavailable.
